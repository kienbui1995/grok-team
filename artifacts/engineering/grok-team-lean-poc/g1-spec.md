# G1 Spec — Story gates (Lean POC)

Stack B, Phase 1. Two surfaces only:

1. Existing grok-team workbench (project / session / diff).
2. Story gates panel/rail overlaid on that workbench.

Gates are sequential: G1 Spec → G2 ADR → G3 Dev (workbench diff) → G4 QC → G5 Demo non-prod.

No factory dashboard, no third route, no App Store, no ship-prod CTA.
